# تقرير الأمان والبنية التقنية - نظام الساقي (AL-SAQI)

> **التاريخ:** 18 مايو 2026  
> **الإصدار:** 1.0.0  
> **مُعد لـ:** قسم تقنية المعلومات  
> **السرية:** داخلي - سري

---

## 1. نظرة عامة على النظام

**نظام الساقي** هو نظام متكامل لإدارة التدقيق الداخلي، المراسلات، تقييم المخاطر، والنزاهة. تم تصميمه ليعمل **بالكامل داخل الخوادم المحلية (On-Premises)** دون أي اعتماد على خدمات سحابية خارجية، مما يضمن السيادة الكاملة على البيانات.

| العنصر | التفاصيل |
|--------|----------|
| نوع التطبيق | تطبيق ويب (Web Application) - SPA |
| النمط المعماري | Modular Monolith |
| الواجهة الأمامية | React 19 + Vite 7 + Tailwind CSS 4 |
| الواجهة الخلفية | Express.js 5 + Node.js 20 + TypeScript 5.9 |
| قاعدة البيانات | PostgreSQL (إنتاج) / PGlite (تطوير) |
| الحاويات | Docker (Node 20 Alpine) |
| الاتصال الفوري | WebSocket (ws) |
| الأتمتة | node-cron |

---

## 2. سياسة عدم الاتصال بالسحابة (Zero-Cloud Policy)

النظام يعمل بسياسة **Air-Gapped** كاملة:

- ❌ لا يوجد اتصال بـ Firebase أو Supabase أو أي مزود هوية سحابي
- ❌ لا يوجد تخزين سحابي (AWS S3, Google Cloud Storage, Azure Blob)
- ❌ لا يوجد مزود مصادقة خارجي (Auth0, Ping, Okta)
- ✅ جميع البيانات مخزنة محلياً على الخادم
- ✅ الملفات المرفوعة تُحفظ في مجلد `/uploads` المحلي
- ✅ مفاتيح التشفير تُولّد وتُخزّن محلياً

---

## 3. طبقات الأمان (Security Layers)

### 3.1 المصادقة (Authentication)

| الميزة | التفاصيل |
|--------|----------|
| طريقة الدخول | اسم مستخدم + كلمة مرور |
| تشفير كلمات المرور | bcrypt (تجزئة أحادية الاتجاه) |
| التوكنات | JWT مُوقّع بـ RSA-256 (تشفير غير متماثل) |
| مفاتيح RSA | تُولّد تلقائياً عند بدء التشغيل وتُحفظ مشفرة بـ AES-256-GCM |
| تجديد الجلسة | Refresh Token مع حد أقصى 30 طلب / 15 دقيقة |
| انتهاء الجلسة | انتهاء صلاحية صارم يفرض إعادة المصادقة |

**آلية عمل المفاتيح:**
1. عند أول تشغيل، يُولّد النظام زوج مفاتيح RSA (عام/خاص)
2. المفتاح الخاص يُشفّر بـ AES-256-GCM باستخدام `JWT_SECRET`
3. يُحفظ الملف المشفر محلياً (لا يحتوي على PEM markers مكشوفة)
4. عند إعادة التشغيل، يُفك تشفير المفتاح ويُستخدم لتوقيع التوكنات

### 3.2 التحكم بالصلاحيات (Authorization)

| الدور | الصلاحيات |
|-------|----------|
| مدير النظام (Admin) | وصول كامل - إدارة المستخدمين والإعدادات والسجلات |
| المدقق الداخلي | تنفيذ خطط التدقيق، تسجيل الملاحظات والأدلة |
| مدير/رئيس التدقيق | تعيين المهام، اعتماد الخطط، مراجعة التقارير |
| المستخدم العادي | عرض محدود - قراءة السياسات وتقديم الإفصاحات |

**نظام الصلاحيات المتقدم:**
- صلاحيات مبنية على الأدوار (Role-Based Access Control - RBAC)
- صلاحيات مخصصة لكل مستخدم (User-Level Permissions)
- فحص الصلاحيات عبر Middleware مخصص لكل Module وAction
- تخزين مؤقت (Cache) لمدة 5 دقائق لتقليل الحمل على قاعدة البيانات

### 3.3 حماية CSRF (Cross-Site Request Forgery)

| الميزة | التفاصيل |
|--------|----------|
| آلية التوليد | `crypto.randomBytes(32)` - 64 حرف hex |
| المقارنة | Timing-Safe Comparison (منع هجمات التوقيت) |
| النقل | Cookie (SameSite: Strict) + Header (`x-csrf-token`) |
| التحقق | مطابقة Cookie مع Header في كل طلب تغيير حالة |
| الاستثناءات | `/api/auth/login`, `/api/auth/refresh`, `/health` |

**الطلبات المحمية:** POST, PUT, PATCH, DELETE  
**الطلبات المستثناة:** GET, HEAD, OPTIONS

### 3.4 تحديد معدل الطلبات (Rate Limiting)

| النطاق | الحد | النافذة الزمنية |
|--------|------|----------------|
| عام (جميع API) | 150 طلب | دقيقة واحدة |
| تسجيل الدخول | 10 محاولات | 15 دقيقة |
| تجديد التوكن | 30 طلب | 15 دقيقة |

**ملاحظة:** مفتاح التحديد لتسجيل الدخول يعتمد على `IP + اسم المستخدم` لمنع حظر مستخدمين آخرين من نفس الشبكة.

### 3.5 رؤوس الأمان (Security Headers)

```
X-Content-Type-Options: nosniff
X-Frame-Options: DENY
x-powered-by: (محذوف)
```

- **nosniff**: يمنع المتصفح من تخمين نوع المحتوى
- **DENY**: يمنع تضمين الصفحة في iframe (حماية من Clickjacking)
- **إخفاء x-powered-by**: يمنع كشف تقنية الخادم

### 3.6 التحقق من الملفات بالذكاء الاصطناعي (AI File Validation)

النظام يستخدم **Google Magika** (نموذج ذكاء اصطناعي) للتحقق العميق من محتوى الملفات:

| المرحلة | الفحص |
|---------|-------|
| المرحلة 1 | التحقق من امتداد الملف ضمن القائمة المسموحة |
| المرحلة 2 | التحقق من حجم الملف (حد أقصى 30MB) |
| المرحلة 3 | تحليل محتوى الملف بالذكاء الاصطناعي (Magika) |
| المرحلة 4 | مطابقة نتيجة AI مع الامتداد المُدّعى |

**الامتدادات المسموحة:**
`.jpg`, `.jpeg`, `.png`, `.gif`, `.webp`, `.pdf`, `.doc`, `.docx`, `.xls`, `.xlsx`, `.txt`, `.csv`

**سيناريو الحماية:** إذا رفع مستخدم ملف `.pdf` لكن محتواه الفعلي هو script تنفيذي، سيكتشف Magika التناقض ويرفض الملف.

### 3.7 التحقق من صحة البيانات (Input Validation)

| الطبقة | الأداة | الوظيفة |
|--------|--------|---------|
| الواجهة الأمامية | React Hook Form + Zod | تحقق فوري من المدخلات |
| الواجهة الخلفية | Zod Schema Validation | تحقق صارم من كل طلب API |
| قاعدة البيانات | Parameterized Queries | منع SQL Injection |

- كل endpoint يستخدم `validateSchema` middleware
- البيانات تُنظّف (Sanitize) قبل الإدخال في قاعدة البيانات
- الحقول الحساسة (IDs, UUIDs) تُحوّل من سلاسل فارغة إلى `null`

---

## 4. أمان جانب العميل (Client-Side Security)

### 4.1 التخزين المشفر (SecureStorage)

جميع البيانات المخزنة في `localStorage` مشفرة:

| الميزة | التفاصيل |
|--------|----------|
| خوارزمية التشفير | AES-256-GCM (Web Crypto API) |
| طول المفتاح | 256-bit |
| طول IV | 12 bytes (معيار GCM) |
| كشف التلاعب | HMAC-SHA256 لكل قيمة مخزنة |
| الاستجابة للتلاعب | حذف البيانات + تسجيل الحادثة |

**آلية العمل:**
1. كل قيمة تُشفّر بـ AES-GCM قبل حفظها
2. يُحسب HMAC-SHA256 للقيمة المشفرة ويُخزّن بجانبها
3. عند القراءة، يُتحقق من HMAC أولاً
4. إذا فشل التحقق → كشف تلاعب → حذف البيانات

### 4.2 حماية الشبكة (SecureNetwork)

نظام اعتراض وحماية لجميع طلبات الشبكة:

| الميزة | التفاصيل |
|--------|----------|
| اعتراض Fetch | تغليف `window.fetch` بطبقة أمان |
| اعتراض XHR | تغليف `XMLHttpRequest` بطبقة أمان |
| توقيع الطلبات | HMAC-SHA256 مع Timestamp + Nonce |
| التحقق من الاستجابة | فحص سلامة Response عبر التوقيع |
| حظر Origins | رفض الطلبات لنطاقات غير مصرح بها |
| كشف XSS | فحص Payload لأنماط `<script>`, `onerror=`, `javascript:` |

### 4.3 حماية الكائنات (ObjectGuard)

حماية JavaScript Runtime من التلاعب:

- **تجميد JSON.parse/stringify**: منع إعادة تعريفها
- **تجميد console**: منع إخفاء السجلات
- **تجميد Math.random**: منع التنبؤ بالقيم العشوائية
- **تجميد Web Crypto API**: منع استبدال دوال التشفير
- **مراقبة Object.defineProperty**: كشف محاولات التلاعب بالكائنات المحمية
- **إبلاغ تلقائي**: إرسال تقرير للخادم عند كشف محاولة تلاعب

---

## 5. تسجيل الأحداث والمراقبة (Logging & Monitoring)

### 5.1 سجل تسجيل الدخول (Login History)

كل محاولة دخول (ناجحة أو فاشلة) تُسجّل:

| الحقل | الوصف |
|-------|-------|
| user_id | معرف المستخدم |
| ip_address | عنوان IP |
| status | نجاح/فشل |
| browser context | معلومات المتصفح |
| timestamp | وقت المحاولة |

### 5.2 سجل التدقيق (Audit Trail)

- تسجيل كل عملية مصادقة (Login, Refresh, Logout)
- تسجيل العمليات الحساسة على البيانات
- إشعارات فورية عبر WebSocket

### 5.3 نظام السجلات (Winston Logger)

- سجلات مهيكلة بمستويات (info, warn, error)
- تسجيل أحداث الأمان (محاولات تلاعب، رفض ملفات، أخطاء مصادقة)

---

## 6. بنية النشر (Deployment Architecture)

### 6.1 Docker Container

```dockerfile
# بناء متعدد المراحل (Multi-Stage Build)
Stage 1: Build (node:20-alpine) → تجميع الكود
Stage 2: Runtime (node:20-alpine) → تشغيل الإنتاج فقط
```

| ميزة الأمان | التفاصيل |
|-------------|----------|
| مستخدم غير جذري | `appuser:appgroup` (UID 1001) |
| صورة مصغرة | Alpine Linux (أقل سطح هجوم) |
| فصل البناء عن التشغيل | Multi-Stage Build |
| تبعيات الإنتاج فقط | `npm ci --omit=dev` |
| تنظيف Cache | `npm cache clean --force` |
| Health Check | فحص صحة كل 30 ثانية |
| Volume منفصل | `/app/data` لفصل البيانات عن الكود |

### 6.2 متغيرات البيئة الحرجة

| المتغير | الغرض | ملاحظة |
|---------|-------|--------|
| `JWT_SECRET` | تشفير مفاتيح RSA المحفوظة | **إلزامي في الإنتاج** |
| `DATABASE_URL` | اتصال PostgreSQL | مع دعم SSL |
| `CORS_ORIGIN` | النطاقات المسموحة | تقييد صارم في الإنتاج |
| `VITE_STORAGE_SECRET` | تشفير التخزين المحلي | 32 حرف على الأقل |
| `VITE_NETWORK_SECRET` | توقيع طلبات الشبكة | HMAC secret |

---

## 7. حماية قاعدة البيانات

| الميزة | التفاصيل |
|--------|----------|
| Parameterized Queries | جميع الاستعلامات تستخدم `?` placeholders |
| Prepared Statements | `db.prepare()` لمنع SQL Injection |
| Auto-Migration | `IF NOT EXISTS` لمنع فقدان البيانات |
| Transaction Support | العمليات المركبة تُنفّذ ضمن Transaction |
| Mass Assignment Protection | حظر تعديل الحقول المحمية (id, created_at, updated_at) |

---

## 8. الأتمتة والمهام المجدولة

| المهمة | التوقيت | الوظيفة |
|--------|---------|---------|
| تحديث حالة خطط التدقيق | يومياً (منتصف الليل) | تفعيل الخطط المجدولة |
| كشف التوصيات المتأخرة | يومياً | تحديث الحالة + إشعار |
| تذكير التوصيات القادمة | يومياً | إشعار قبل 7 أيام |
| تحديث تعليمات البنك المركزي | يومياً | كشف التأخير |
| مراجعة السياسات السنوية | يومياً | تنبيه عند الاستحقاق |
| تذكير المهام | يومياً | إشعار قبل 3 أيام |

---

## 9. الاتصال الفوري (Real-Time)

- **البروتوكول:** WebSocket (ws library)
- **الاستخدام:** إشعارات فورية للمستخدمين المتصلين
- **الآلية:** عند حدوث حدث (مهمة جديدة، تذكير)، يُرسل إشعار مباشر لكل مستخدم مستهدف عبر WebSocket

---

## 10. حدود الحمولة وحماية DoS

| الحد | القيمة |
|------|--------|
| حجم JSON Body | 30MB |
| حجم URL-Encoded | 30MB |
| حجم الملف المرفوع | 30MB |
| طلبات API/دقيقة | 150 لكل IP |
| محاولات دخول | 10 لكل IP+مستخدم / 15 دقيقة |

---

## 11. التقنيات والمكتبات الأمنية المستخدمة

| المكتبة | الإصدار | الوظيفة |
|---------|---------|---------|
| `bcryptjs` | 3.0.3 | تجزئة كلمات المرور |
| `jsonwebtoken` | 9.0.3 | إنشاء والتحقق من JWT |
| `express-rate-limit` | 8.3.1 | تحديد معدل الطلبات |
| `cors` | 2.8.6 | التحكم بالنطاقات المسموحة |
| `cookie-parser` | 1.4.7 | قراءة Cookies بأمان |
| `magika` | 1.0.0 | تحليل محتوى الملفات بالذكاء الاصطناعي |
| `zod` | 4.3.6 | التحقق من صحة البيانات |
| `winston` | 3.19.0 | تسجيل الأحداث المهيكل |
| `Web Crypto API` | (مدمج) | تشفير AES-GCM + HMAC-SHA256 |

---

## 12. ملخص مستوى الأمان

| الفئة | التقييم | ملاحظات |
|--------|---------|---------|
| المصادقة | 🟢 عالي | RSA-256 + bcrypt + Rate Limiting |
| التفويض | 🟢 عالي | RBAC + صلاحيات مخصصة + Cache |
| حماية CSRF | 🟢 عالي | Double-Submit + Timing-Safe |
| حماية الملفات | 🟢 عالي | AI-based validation (Magika) |
| تشفير البيانات | 🟢 عالي | AES-256-GCM + RSA + HMAC |
| حماية العميل | 🟢 عالي | SecureStorage + SecureNetwork + ObjectGuard |
| سيادة البيانات | 🟢 عالي | Zero-Cloud / On-Premises |
| تسجيل الأحداث | 🟢 عالي | Login History + Audit Trail |
| حماية DoS | 🟡 متوسط | Rate Limiting (يُنصح بإضافة WAF) |
| HTTPS/TLS | ⚪ خارجي | يعتمد على Reverse Proxy (Nginx/Traefik) |

---

## 13. التوصيات المستقبلية

1. **إضافة Helmet.js** لرؤوس أمان إضافية (CSP, HSTS, Referrer-Policy)
2. **تفعيل HTTPS/TLS** عبر Reverse Proxy (Nginx أو Traefik)
3. **إضافة WAF** (Web Application Firewall) أمام التطبيق
4. **تفعيل DB SSL** (`DB_SSL_REJECT_UNAUTHORIZED=true`)
5. **مراجعة دورية للتبعيات** عبر `npm audit`
6. **إضافة 2FA** (المصادقة الثنائية) للحسابات الحساسة
7. **تشفير الملفات المرفوعة** على القرص (Encryption at Rest)
8. **إضافة IP Whitelisting** للوصول الإداري

---

> **أُعد هذا التقرير بناءً على تحليل الكود المصدري للنظام.**  
> **للاستفسارات التقنية، يرجى التواصل مع فريق التطوير.**
